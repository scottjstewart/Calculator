import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => {
  return {
    result: {
      height: "60px",
      backgroundColor: "#bbb",
      width: "100%"
    },
    display: {
      margin: "5px",
      fontSize: "40px"
    }
  };
});
