import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => {
  return {
    buttonContainer: {
      display: "block",
      backgroundColor: "#bbb"
    },
    button: {
      width: "25%",
      height: "60px",
      fontSize: "30px"
    }
  };
});
